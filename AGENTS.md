# Friday 工程规则

## 项目背景

Friday 是一个原生 macOS AI 助手，当前项目位于 `Friday.xcodeproj`，最低部署目标为 macOS 14.0。产品优先验证“自然语言输入 → 口语整理 → 写回当前输入框”，之后再扩展屏幕理解和 Agent。

产品路线、用户故事和阶段门槛见 `docs/产品路线图.md`；人机协作方式见 `docs/工程协作规范.md`。每次跨阶段开发前先读取这两个文件。

## 每次开始前先看

- `README.md`
- `docs/产品路线图.md`
- `docs/工程协作规范.md`
- `docs/编程Agent规范.md`
- `docs/Issue与PR工作流.md`
- `docs/工程问题与复盘.md`
- `docs/项目结构.md`
- `Friday/FridayApp.swift`
- `Friday/ContentView.swift`
- `Friday.xcodeproj/project.pbxproj`

如果进入已有的 Platform、Core 或 Features 子模块，再阅读该模块的源码和 README。上下文被压缩、切换任务或不确定当前实现时，重新阅读，不靠记忆猜测。

## 沟通与工作流

- 默认中文回复，先结论后细节。
- 开始编辑前说明修改范围；完成后说明改动、验证、风险和待人工检查项。
- 默认直接实施用户要求的本地代码和文档工作；用户明确说“只讨论/只分析”时不改代码。
- 不确定时写明假设；会改变产品方向、隐私或费用的选择必须先确认。
- 不把官网宣传、模型猜测或未在真实 Mac 上验证的行为写成已实现事实。

## 准确性与证据

- 用户和 Codex 的判断都可能有误；以源码、配置、命令结果、测试和真实 Mac 验收作为优先证据。
- 明确区分“产品计划”“代码已实现”“自动检查通过”和“真机验收通过”，不相互替代。
- 涉及模型名称、API 价格、系统限制和第三方能力等时效信息时，优先查官方一手资料并注明查询日期。
- 文档、代码和用户描述冲突时，先指出具体冲突并核实；不能核实时采用影响最小、可撤销的方案。
- 修复问题前先复现或找到可验证证据；无法复现时说明观察范围，不把推测当成根因。

## 工程问题与复盘

- 遇到反复出现、跨模块、根因不明显，或涉及音频、异步状态、系统权限、网络、隐私、费用和数据安全的问题时，排查开始即在 `docs/工程问题与复盘.md` 建立条目。
- 记录随调查更新，保留用户现象、证据、假设、失败实验、根因、修复、验证和可复用经验；不能等解决后只补一个结果摘要。
- 调查中的判断标为假设；没有充分证据时不得填写确定根因或标记“已解决”。
- 不把 API Key、原始音频、完整对话、完整转写、完整截图或用户私有内容写入复盘文档。
- Issue 负责范围与验收，PR 负责实现与审查，路线图决策记录负责方向选择；复盘文档负责保留问题如何被定位和解决，四者不能互相替代。

## 产品优先级

1. 通用输入框注入：当前焦点、Accessibility、剪贴板后备、撤销。
2. 实时语音改写：Mock 与真实 Provider 解耦、临时凭证、成本控制。
3. 按需屏幕上下文：用户主动触发、可见提示、无持续监控。
4. 受控 Agent：工具调用、风险等级、预览、确认、取消和恢复。

不要在前一层还不稳定时跳到后一层；不要为了演示而绕过权限和确认边界。

## 架构边界

- `App/`：入口、菜单栏和生命周期。
- `Features/`：用户可见能力和页面状态。
- `Platform/`：Accessibility、全局快捷键、ScreenCaptureKit、音频和系统窗口。
- `Core/`：Provider、权限、错误、配置和状态编排。
- `Shared/`：共享模型、样式和格式化工具。
- SwiftUI 视图不直接管理 API Key、WebSocket、Accessibility 树或音频设备。
- Realtime Provider 必须有 Mock 实现；界面不能依赖真实 API 才能开发和测试。
- API、模型和中转服务必须通过 Provider 接口隔离，避免锁定单一供应商。

## 安全与隐私

- 长期 API Key 只能放在服务端，不能写入 `.app`、UserDefaults、源码或日志。
- 客户端使用后端签发的短期 Realtime 凭证。
- 默认不持续读取屏幕；屏幕捕获只在用户主动触发后进行。
- 不在日志中写入原始音频、完整截图、完整转录、密码、Token 或 Authorization header。
- 发送、删除、购买、提交、权限变更和不可逆操作默认需要确认。
- 任何用户内容缓存都要明确保存目的、保留时间和删除方式。

## 修改与测试规则

- 优先编辑现有文件，只在职责清晰且确有必要时新建文件。
- 新增或替换界面图标统一从 [Iconsax App](https://app.iconsax.io/) 取得，默认使用 `Rounded + Bold`；同一控件组必须使用相同的 Corner 与 Style，确需例外时先说明原因并在资源索引中记录。
- 普通界面图标统一保存为 128×128 透明 PNG，并在 `docs/项目结构.md` 记录 Iconsax 图标名称、Corner、Style 和来源链接；不使用其他图标网站的资源混入现有控件组。
- 所有 Swift、MJS 和 Shell 源码文件必须在顶部详细说明“功能、职责、边界”；Shell 的 shebang 保持第一行，具体格式见 `docs/编程Agent规范.md`。
- 新建、删除、重命名或移动代码文件时，必须同步更新文件头和 `docs/项目结构.md`；文件的主要职责、依赖边界、权限、数据流或外部副作用变化时同样必须同步。
- 仅实现细节变化且文件职责未变时，不为制造文档改动而重写文件头或项目结构说明。
- 文件超过 800 行时新增功能前先评估拆分；超过 1200 行原则上拆分。
- 新增用户行为必须有用户故事验收条件；纯技术任务要写清验证信号。
- 在完整 Xcode 环境中优先运行构建；存在可执行的测试 Target 时再运行测试：

```bash
xcodebuild -project Friday.xcodeproj -scheme Friday -configuration Debug -destination 'platform=macOS' build
xcodebuild test -project Friday.xcodeproj -scheme Friday -destination 'platform=macOS'
```

- 当前 Demo 阶段至少做 Swift 语法解析和 `plutil -lint Friday/Info.plist`；不能把静态检查称为完整构建。
- Accessibility、麦克风、屏幕录制和 Realtime 必须在真实 Mac 上人工验证。

## 本地语音服务交付检查

- 每次完成本地代码修改后，运行 `cd Backend && npm run status` 检查 `127.0.0.1:8787`。
- 如果端口未监听，启动 `cd Backend && npm start`；管理脚本固定只读取 macOS 钥匙串，并让进程在用户测试期间保持运行，不要求用户重复手动启动。
- 分开报告“本地服务已监听”和“OpenAI 模型检查已就绪”。网络、代理、Key 或模型权限失败时，保留本地服务并说明具体失败证据。
- 自动交付检查只能调用健康检查或模型元数据接口；未经用户明确要求，不创建 Realtime 凭证、不发送音频，也不触发任何付费模型响应。
- 如果本次修改涉及 `Backend/` 运行时代码，提醒当前已运行的旧进程需要重启；不能把旧进程仍在监听误认为新代码已生效。

## Git 与外部操作红线

- 保留用户未相关改动，不使用 `git reset --hard` 或 `git checkout --` 覆盖文件。
- 新功能、Bug、架构、跨模块契约、隐私、权限或费用调整必须由 Issue 定义问题、范围和验收条件；纯注释、错别字等极小维护可豁免，但要在 PR 中说明。
- 基线建立后，每项代码或长期文档变更使用独立分支和 PR；一个 PR 只处理一个可独立验收、审查和回滚的结果，并使用 `Closes #<issue>` 关联 Issue。
- Codex 分支默认使用 `codex/<issue-number>-<short-slug>`；详细流程、模板和初始化例外见 `docs/Issue与PR工作流.md`。
- 未经用户明确授权，不在 GitHub 创建 Issue 或 PR；没有外部操作授权时只准备本地分支、提交和完整描述，不虚构编号或链接。
- 未经用户明确说“推送 / push”，绝不执行 `git push`。
- 不擅自购买 API、充值中转站、提交外部表单、上传用户数据或发布 DMG。
- 发布版本需要用户明确授权，并额外完成签名、公证、权限引导、隐私说明和回滚检查。

## 交付格式

每次完成任务时，说明：

1. 改了哪些文件和用户行为。
2. 做了哪些自动检查或真机验证。
3. 哪些验证因为环境或权限未完成。
4. 仍存在的风险、假设和下一步。
